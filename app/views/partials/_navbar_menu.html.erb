<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#the-navbar" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Boodka', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="the-navbar">
      <%= navbar do %>
        <%= navbar_item caption: 'Accounts', path: accounts_path %>
        <%= navbar_item caption: 'Budget', path: current_period_path, enabled: Account.any? && Category.any? %>
        <%= navbar_item caption: 'History', path: current_operations_path %>
      <% end %>

      <%= navbar side: :right do %>
        <%= navbar_item caption: 'New transaction', path: new_transaction_path, enabled: Account.count > 0 %>
        <%= navbar_item caption: 'Transfer', path: new_transfer_path, enabled: Account.count > 1 %>
        <%= navbar_item caption: 'Reconcile', path: new_reconciliation_path, enabled: Account.count > 0 %>
      <% end %>
    </div>
  </div>
</nav>
