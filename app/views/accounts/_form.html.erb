<%= form_for account_form, url: submit_url, method: method, html: { class: 'form account-form' }, builder: SmartBuilder do |form| %>
  <%= form.text :title, placeholder: 'Account title', class: 'select-on-focus', autofocus: true %>

  <% if account_form.persisted? %>
    <%= form.static :currency %>
  <% else %>
    <%= form.select :currency, options: options_for_select(Const::CURRENCY_CODES) %>

    <div class="form-group">
      <div class="btn-group btn-group-sm" role="group">
        <a href="#" class="btn btn-sm btn-default currency-switch" data-value="USD">USD</a>
        <a href="#" class="btn btn-sm btn-default currency-switch" data-value="GBP">GBP</a>
        <a href="#" class="btn btn-sm btn-default currency-switch" data-value="EUR">EUR</a>
        <a href="#" class="btn btn-sm btn-default currency-switch" data-value="RUB">RUB</a>
      </div>
    </div>

    <%= form.text :amount, placeholder: 'Amount' %>

    <%= form.datetime :reconciled_at, placeholder: 'Default is now' %>
  <% end %>

  <%= form.area :memo, placeholder: 'Optional notes' %>

  <%= form.check :default, 'Set default' %>

  <%= form.submit_group caption: submit_caption, cancel: true %>
<% end %>

<script>
  $(function() {
    "use strict";

    $(".account-form .select-currency").select2({
      theme: "bootstrap",
      allowClear: false
    });


    $(".currency-switch").click(function() {
      $(".select-currency").val($(this).data("value")).trigger("change")
    });

    $(".date-time-picker").datetimepicker({
      format: "<%= Const::DATEPICKER_FORMAT_MOMENTJS %>"
    });
  });
</script>
