$("#the-modal .modal-dialog").html("<%=
  j render(partial: 'form', locals: {
    account_form: @form,
    submit_url: accounts_path(format: :js),
    method: :post,
    form_caption: 'New account',
    submit_caption: 'Create new account',
    edit: false
  }) %>");

$("#the-modal").modal();
