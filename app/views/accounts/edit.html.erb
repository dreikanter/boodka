<%=
  render partial: 'form', locals: {
    account_form: @form,
    submit_url: account_path(@form.id),
    method: :patch,
    form_caption: 'Account details',
    submit_caption: 'Update',
    edit: true,
    cancel: accounts_path
  }
%>
