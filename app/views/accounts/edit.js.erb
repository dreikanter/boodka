$("#the-modal .modal-dialog").html("<%=
  j render(partial: 'form', locals: {
    account_form: @form,
    submit_url: account_path(@form.id, format: :js),
    method: :patch,
    form_caption: 'Account details',
    submit_caption: 'Update',
    edit: true
  }) %>");

$("#the-modal").modal();
