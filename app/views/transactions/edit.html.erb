<%= render partial: 'form', locals: {
  transaction: @transaction,
  submit_url: transaction_path(@transaction.id),
  method: :patch,
  remote: false,
  form_caption: 'Transaction details',
  submit_caption: 'Update transaction'
} %>
